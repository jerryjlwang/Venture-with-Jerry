// Stable asset version. Bump manually when a published image changes so the
// browser picks up the new bytes. Using a per-load value (e.g. Date.now())
// would defeat the browser cache on every page load.
export const ASSET_V = '1';
